# Event: {eventName}

<!--
========================================================================
TEMPLATE — PUBLIC MQTT EVENT (device-emitted) reference.
========================================================================
End-user / developer-facing reference for ONE device-emitted event: the device
PUBLISHES it autonomously (NOT in reply to a request), on an INTERNAL TRIGGER or
at a PRE-SET TIMED INTERVAL. The app SUBSCRIBES. No request, no response envelope,
no `response.code`. (The broker only routes — never write "the broker sends".)

HOW TO USE: copy this file, rename it `{eventName}.mdx`, add it to the API
reference sidebar (see PLACEMENT below), fill every {placeholder}, then DELETE
every HTML comment (MDX uses {/* */}).

BODY DESIGN — <ApiBody> (Schema tab + Example tab):
The Event payload and Configuration are each ONE <ApiBody rows={...} json={...} />,
which renders two tabs of the SAME data:
  • "Schema"  — a unified collapsible tree-table (Field · Type · Required · Enum /
                Units · Example · Description) with a line-number gutter, `+`/`−`
                toggles, and Expand all / Collapse all.
  • "Example" — a collapsible, copy-pasteable JSON block with line numbers, `+`/`−`
                folding, Expand all / Collapse all, and a Copy button.
Define the row tree(s) and JSON object(s) ONCE in the ESM block as `export const`.

PLACEMENT — the API reference is a SEPARATE doc: its own "API Reference" top-nav tab
and its own `apiReference` sidebar in `sidebars.ts` (pages keep /reference/... URLs).
Add this page's id to the `apiReference` sidebar. The right-hand TOC is persistent;
wide schema tables get room at the table level inside `<SchemaTable>` (nothing per page).

INTERNAL CONTENT LIVES ELSEWHERE: live-capture verdicts / provenance live in
`{eventName}.maintainer.md`.

MDX AUTHORING RULES:
  - Replace ALL {placeholders}. DELETE every HTML comment (use {/* ... */} to keep one).
  - Define row trees / JSON objects in the ESM block as `export const` (plain JS; single-quote
    strings; backtick-delimited spans in a `desc` render as <code>).
  - In PROSE/TABLE CELLS, literal { } and < are parsed as JSX — wrap in `backticks` or escape.
  - Blank line after each opening JSX tag and before each closing one (inside <Variant>).
  - Do NOT put a :::admonition inside a component.

SCHEMA ROW SHAPE: { field, type, required, enumUnits, example, desc, children }
  - Some event schemas have NO `required` array — mark accordingly; do NOT assert required
    if the schema does not.

An event payload has NO response/code object. Events are read-only telemetry — usually nothing
to mask; if a payload ever carries a secret, mask it as ********.
========================================================================
-->

{/* Required MDX imports — components live in src/components (see TEMPLATE.assets.md) */}
import ApiBody from '@site/src/components/ApiBody';
{/* Keep if you add a standalone JSON example (e.g. a second emitted payload) in the Examples section; otherwise delete. */}
import JsonBlock from '@site/src/components/JsonBlock';
{/* Only needed if the event emits MULTIPLE payload shapes (see Event payload). Delete otherwise. */}
import { VariantSelect, Variant } from '@site/src/components/VariantSelect';

{/*
  ESM DATA — payload table tree + emitted JSON, and (if gated) the configuration table tree +
  enable JSON. Multi-shape event: define one PAYLOAD_ROWS_*/PAYLOAD_JSON_* pair per shape.
*/}
export const PAYLOAD_ROWS = [
  { field: 'eventName', type: 'string', required: true, example: '{eventName}', desc: 'Names the event; use it to route/filter incoming messages.' },
  { field: 'timestamp', type: 'string', required: true, enumUnits: 'date-time (ISO 8601)', example: '{ISO-8601 timestamp}', desc: 'Time the device generated the event.' },
  { field: 'data', type: 'object', required: false, desc: 'Payload the event carries.', children: [
    { field: '{subField}', type: 'string', required: false, example: '{value}', desc: 'What it reports.' },
    { field: '{status}', type: 'enum', required: false, enumUnits: ['VALUE_A', 'VALUE_B'], example: 'VALUE_A', desc: 'A constrained value.' },
    { field: '{count}', type: 'integer', required: false, enumUnits: '{units}', example: '1', desc: 'A count/measurement.' },
  ] },
];

export const PAYLOAD_JSON = {
  eventName: '{eventName}',
  timestamp: '{ISO-8601 timestamp}',
  data: { '{subField}': '{value}' },
};

export const CONFIG_ROWS = [
  { field: '{enable_flag}', type: 'boolean', required: false, example: 'true', desc: 'Set `true` to enable emission.' },
  { field: '{configBlock}', type: 'object', required: false, desc: 'Per-event settings.', children: [
    { field: 'interval', type: 'integer', required: false, enumUnits: 'seconds', example: '60', desc: 'Interval in seconds (must be `> 0` for a timed event); omit for trigger/streamed events.' },
  ] },
];

export const CONFIG_JSON = {
  command: 'config_events',
  requestId: '{requestId}',
  eventConfiguration: { '{enable_flag}': true, '{configBlock}': { interval: 60 } },
};

## Overview

<!-- 1–2 sentence headline of what {eventName} reports and why a subscriber cares. State plainly:
device-emitted, NO request/response, NO response.code, and whether it fires on an INTERNAL TRIGGER
or a TIMED INTERVAL. -->

`{eventName}` is a device-emitted event: the device publishes it on its own — no command triggers it — to report {what it reports}. It fires on **an internal trigger** (e.g. a state change) **or at a pre-set timed interval** — state which. There is no request, no response envelope, and no `response.code`.

:::info
Read-only telemetry — receiving `{eventName}` changes nothing on the device. Emission is **gated by configuration** (see [Configuration](#configuration)); if you see none, check that gate first.
:::

## Details

| Property | Value |
| --- | --- |
| **Pattern Name** | Event (device-emitted, fire-and-forget; no reply) |
| **Applies To** | {endpoint/plane — e.g. MGMT `event` or DATA `data1event`}; {device models — e.g. RFD40, RFD90} |
| **Related Commands / Events** | {e.g. the enable command [`config_events`](/reference/mgmt/event-configuration); sibling events} |
| **Emission** | {INTERNAL TRIGGER (e.g. a state change) or TIMED INTERVAL (every `{interval_seconds}` s)} |
| **Gated By** | {e.g. `{enable_flag}=true` + interval via `config_events`; or a START trigger; or none (autonomous)} |
| **State Change** | READ-ONLY telemetry (receiving it changes nothing) |
| **Response Code** | None — device-emitted events carry no `response.code` |

## When to Use

<!-- 2–4 sentences/bullets: WHAT info the subscriber gets, for WHAT purpose, in WHAT context,
and WHAT problem it solves. -->

Subscribe to `{eventName}` when you need to **{outcome}** — {for what purpose}, typically in **{context / environment}**. It solves **{the problem}** without polling.

- **Monitor / detect:** {what state or data it surfaces}.
- **Use it for:** {the concrete scenario(s)}.
- **Prefer a polling command when:** {e.g. you need on-demand state — point to the matching `get_*` command}.

## Topic Mapping

<!-- The DEVICE publishes, the APP subscribes. The role segment is endpoint-specific. Wrap literal
{serial} / <tenantId> tokens in backticks. -->

| Direction | Topic | Note |
| --- | --- | --- |
| Device-emitted event (publish by device; subscribe by app) | `zebra/MGMT/clients/event/{serial}` | Which plane/endpoint emits it |

:::note
The role segment is endpoint-specific: the MGMT plane uses `zebra/MGMT/clients/event/{serial}`; the DATA plane uses `zebra/DATA/clients/data1event/{serial}` (the role tail is `data1event`, not `event`). There is no per-message authentication — credentials are presented once at CONNECT.
:::

## Key Fields

- **`eventName`** — identifies the event (`{eventName}`); use it to route/filter incoming messages.
- **`timestamp`** — when the device generated the event.
- **`data`** — {the payload the subscriber consumes; name the key sub-object(s)}.
- **`{enable_flag}`** (config) — the gate that must be set for this event to emit (see [Configuration](#configuration)).

## Event payload

<!-- The emitted payload — NO response/code object. Render as ONE <ApiBody>.

SINGLE-SHAPE (most events): keep the single <ApiBody> line below.

MULTI-SHAPE (different `data` sub-objects or per-plane variants): wrap per-shape <ApiBody> in a
standalone <VariantSelect label="Payload variant">. Define one PAYLOAD_ROWS_*/PAYLOAD_JSON_* pair
per shape. Leave "Required" per the schema's `required` array — some event schemas have none. -->

<ApiBody rows={PAYLOAD_ROWS} json={PAYLOAD_JSON} title="Event payload" />

{/* MULTI-SHAPE alternative (delete the <ApiBody> line above and use this shape):

<VariantSelect label="Payload variant">

<Variant value="a" label="{Variant A}">

<ApiBody rows={PAYLOAD_ROWS_A} json={PAYLOAD_JSON_A} title="Event payload" />

</Variant>

<Variant value="b" label="{Variant B}">

<ApiBody rows={PAYLOAD_ROWS_B} json={PAYLOAD_JSON_B} title="Event payload" />

</Variant>

</VariantSelect>
*/}

## Configuration

<!-- How to ENABLE emission — events are gated. Render the enable payload as ONE <ApiBody>.
TIMED event: an enable flag + interval seconds via config_events (or config_endpoint), usually
applied on REBOOT. STREAMED/trigger event: a START trigger instead of an interval; some events
emit autonomously with no configuration (say so and delete the <ApiBody> below). -->

<ApiBody rows={CONFIG_ROWS} json={CONFIG_JSON} title="Request payload" />

:::note
The enable is delivered via `config_events` (or `config_endpoint`) and typically **applies on device reboot**. For a streamed event there is no interval — arm it with a START trigger (e.g. `control_operation` START); some events emit autonomously with no configuration at all.
:::

## Examples

<!-- The Event payload <ApiBody> already shows the full emitted JSON on its Example tab. Keep this
concise: how to recognize the event (its `eventName`), and — if useful — a second realistic payload
for a different state via a <JsonBlock>. An event has no request/response pair. -->

`{eventName}` arrives unsolicited on the topic above; match on `eventName` (`{eventName}`) to route it. The payload shape is on the [Event payload](#event-payload) tabs.

## Notes & Caveats

<!-- Cover the no-response-code fact, troubleshooting when no events arrive, and read-only /
nothing-to-mask. Delete any you don't need. -->

:::info
`{eventName}` is device-emitted and carries **no `response.code`** — the `refrence/response/response.yaml` code table does not apply to events. The *absence* of an expected event is the signal to investigate.
:::

:::tip
If no `{eventName}` arrives: confirm the gate (`{enable_flag}=true` and `{configBlock}.interval > 0` for a timed event, or that the stream was armed with START); confirm you subscribed to the right plane/topic; remember config changes may apply only after a reboot.
:::

:::note
Read-only telemetry — the payload carries no credentials, so there is nothing to mask. If enabling this event changed stored config, revert by re-sending `config_events` with `{enable_flag}=false` (or a different interval).
:::

**Related:** 📘 [{concept chapter}](/…) · 📕 [Event configuration](/reference/mgmt/event-configuration) · 📕 [All events](/reference/events/all-events)
