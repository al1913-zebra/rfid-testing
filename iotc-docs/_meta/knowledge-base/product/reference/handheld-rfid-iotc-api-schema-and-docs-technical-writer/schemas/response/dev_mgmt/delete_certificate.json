{
  "title": "deleteCertResponse",
  "x-stoplight": {
    "id": "t528jqvetxxjk"
  },
  "type": "object",
  "examples": [
    {
      "command": "delete_certificate",
      "requestId": "abc123",
      "apiVersion": "V1.1",
      "response": {
        "code": 0,
        "description": "Success"
      }
    }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "delete_certificate",
      "description": "The command that was executed to delete a certificate."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "The unique identifier of the original request."
    },
    "apiVersion": {
      "type": "string",
      "example": "V1.1",
      "enum": [
        "V1.0",
        "V1.1"
      ]
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml"
    }
  },
  "required": [
    "command",
    "requestId",
    "apiVersion",
    "response"
  ]
}