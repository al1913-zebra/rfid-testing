{
  "title": "installCertificate",
  "x-stoplight": {
    "id": "pyaqnm42or6o5"
  },
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "example": "install_certificate",
      "description": "The command that was executed to install a certificate."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "The unique identifier of the original request."
    },
    "apiVersion": {
      "type": "string",
      "enum": [
        "V1.0",
        "V1.1"
      ]
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml"
    }
  },
  "examples": [
     {
       "command": "install_certificate",
       "requestId": "18996",
       "apiVersion": "V1.1",
       "response": {
           "code": 1,
            "description": "Command payload is accepted"
        }
     }
  ],
  "required": [
    "command",
    "requestId",
    "apiVersion",
    "response"
  ]
}