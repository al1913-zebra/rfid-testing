title: kill
type: object
description: Defines the kill command payload used to permanently disable an RFID tag.
examples:
  - type: KILL
    config:
      password: string
properties:
  config:
    type: object
    required:
      - password
    properties:
      password:
        type: string
        description: |-
          Password Field (see EPC Gen2 Spec)
          Must be a hex string with 8 hex characters. 
required:
  - config
