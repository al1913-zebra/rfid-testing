This section serves as a guide to migrating the `FxConnect` settings to `IoT Connector`.

Important

Currently, IoT Connector supports HTTP POST and USB HID modes that are supported in Fxconnect. USB Flash drive and TCP/IP are not supported.