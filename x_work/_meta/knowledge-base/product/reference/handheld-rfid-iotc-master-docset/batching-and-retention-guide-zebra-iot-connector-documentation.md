# Batching & Retention Guide — capture stub (no SoT schema)

> **No usable content** — a MarkSnip browser-capture stub. Retained only as a redirect so it stops reading as coverage in source audits.
>
> The reader-side retention buffer is **not part of the MQTT API schema** — its constants (e.g. buffer size, flush rate) are firmware-dependent. **Published page:** `docs/fleet/retention-and-retry.md`, which flags those figures as firmware-dependent. See `_meta/governance/ia-blueprints/actions-v2.md` §6 (C1).
