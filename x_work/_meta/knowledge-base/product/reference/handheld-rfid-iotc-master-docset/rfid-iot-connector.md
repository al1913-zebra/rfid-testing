hey welcome back everyone and welcome to our penultimate session so we have harsher with us again today for his second session of the day and as you'll all be aware we'll be covering the iot connector that's enough of me over to harsha this stage is yours thank you okay thanks james um welcome back everyone this in this session we will cover the iot connector and um let's walk through all the different uh wonderful things that we have to offer in either connector uh all right uh just setting agenda in terms of what we will try to cover in this session again i'll walk through the features and highlights of the iit connector um what are the various things on offer we'll also look at the system overview uh which is basically to see what different components are part of the iot connector and watch what each of them does and we'll also look at the deployment modes the deployment modes in terms of the pure cloud deployment online deployment offline regardless of the on-prem deployment and the hybrid deployment modes uh we'll talk about the operating modes uh that are supported by the it collector we'll also talk about how you can control gpus and leds using the iot connector and the other thing that i i'm personally very excited about is the da apps so we are introducing a new feature with id connector called the data analytics applications uh which will allow the users to perform all sorts of different uh filtering and other analytics that they want to perform on the reader to using the id connector so this this brings the analytics capability to the edge and that's what uh i want to walk over with the app framework over here um and then finally we'll run through a quick demo i'll uh show how to set up the id connector at different endpoints start type rate stop tag reads and how you can view the tag reads and so on okay starting off uh for features and highlights uh it connector is supported on the following readers so we support it on the fx 7500 fx 9600 and atr 7000 um so what id connector does is it provides a very easy to use cloud friendly interface uh to the readers and typically when you are working with a reader if you are using either the rm or lnrp in both these cases what happens is that the the application that wants to control and manage the readers would need to make a connection to the reader which is obviously a sort of a drawback if you are want to manage the readers from the cloud because uh most of the leaders when they are deployed behind the corporate firewalls do not allow external locations to go in and connect to the readers so what iot connector does is it flips that paradigm instead of the applications connecting to the reader the reader is the one that initiates the connections and is uh sending out data and that's makes that makes the readers uh very cloud friendly and provides a lot of iot capabilities that you can leverage um so in terms of iot connect capabilities there are iot connector provides four different interfaces through which you can uh talk to the reader uh one is called the management interface uh there's the control interface there's a data interface and then there's the monitoring interface uh typically i like to think of it as synchronous and asynchronous interfaces um synchronous being when you send a command and get back a response so your management and control are your synchronous interfaces where you get back a response only on a certain command being sent whereas the data and monitoring interfaces are asynchronous interfaces and these are initiated by the reader so the data coming out of the reader or he or the management coming out of the reader management events coming out with the reader uh are all initiated by the reader without the user having to send a command to the reader so those are asynchronous interfaces um the other feature that is part of the id connector is the data retention um so i think we talked about a little bit in the previous session how llrp buffers data in case of um it not being consumed by the user fast enough uh what id connector provides is similar uh but instead of the consumption uh what iot connector does is if it is not able to send the data events to the endpoint that is configured for data it will start buffering it on the reader itself and it will retain that data until the network connection is restored and once the network connection is restored it will send all the data back so you don't lose any data in addition to it you can also configure the throttle uh at which the data is sent back so if you if the network is down for a long time for example and if the reader is continuously reading tags uh there is a possibility that it starts accumulating a lot of tags on the reader and it and when the network connection is restored it will just uh pump everything so in order to avoid that that could that has a potential to bring down the uh the cloud service which is managing all these different readers depending on how many readers there are right so in order to avoid that what the feature in the iot connector uh does is it provides the ability to throttle so you can specify once the network connection is restored uh at what rate do we want these events to be sent back to the cloud uh so that it doesn't bring that bring down the cloud application um so that is configurable as well that's one of the features that in the id connector um and then yeah obviously it supports the radio operating modes operating modes is similar to um here what the operating mode uh sort of replaces what you would do with an llrp uh in the conventional case where you configure the way in which you you want the reader to read tags or do any sort of pre-filters post filters or if you want to configure a gpi trigger for the reads for example uh so operating mode sort of encompasses all the functionality that llrp provides into a simple configurable json object that can be sent to the reader and you can easily specify or configure what how you want the reader to behave how you wanted to filter how you wanted the triggers to be handled what type metadata you want it to report all that sort of things so there are again predefined operating modes there are four very different operating modes we'll run through them uh in the slides further uh but those four operating modes are they do provide some customization knobs that you could adjust based on the use case but then those operating modes provide or rather will inhibit you from using certain knobs depending on which operating mode you are because of the nature of the definition of the modes but if you don't want those predefined modes if you want full flexibility reader provides something called a custom mode which is very uh expert level configuration where all the knobs that are available are available to the user uh to adjust and that can be used to achieve any functionality that you would otherwise uh have their reader do by an rp and then in terms of deployment modes we support uh i mean the reader supports fully cloud deployment where you are completely managing the reader controlling the reader and consuming the tag coming out of the reader from cloud and there is an on-prem deployment where the reader if the readers are in located in a network environment where connection to an internet is not possible then it can do a fully on-prem deployment there are also hybrid operation modes where you could manage and control the reader from the cloud but consume the tag data locally uh to avoid uh expenses that are incurred with high network bandwidth utilization so that's uh we'll run through those as well uh the reader supports two simultaneous data paths so um in terms of the data parts that are supported we support http post mqtt and uh we support keyboard emulation and a bunch of cloud providers as well we support gcp aws um ibm and azure so the two data paths the simultaneous data paths that are supported can be sent to any of the supported endpoints so you could have the various combinations there uh i'll demo that later as well and yeah i think like i said we are introducing the da app framework here uh what this allows you to do one of the things that at least they have allow you to do is if you want to uh consume data and send it to two separate endpoints but you don't want all the data to be sent to both the end points uh you want different data to send listen to different endpoints that can be done from the dea application so typically what would happen is if you don't want all the data to go to the cloud if you're sensitive to the network bandwidth utilization so you could filter out the data that goes to the cloud but if there is a local connection that you are the data endpoint is added as well then reader can send the unfiltered data to a local endpoint uh whereas the filter data can go to the uh cloud so that that sort of a paradigm is possible using the app as well uh and yeah the you the framework is available in two language bindings we support python and node.js so you users can write any applications using either of these languages and deploy onto the reader and that will be started in the i mean basically it will perform the filtering operations that you would want on the reader all right um a quick system overview so i these are the different components that are part of the iot connector so there is a uh reader gateway or a cloud agent uh the other component is called the reader control um and the third component is the reader manager so these are the three main components that are responsible for implementing either connector within the reader the reader gateway uh is the one that is responsible for interfacing to the outside world so any uh connections that it makes for management control and data are all held by the data gateway and that is the one that translates the iot connector commands that are coming in on those interfaces to the respective uh components so if there are management functionalities that are being asked of the reader to be performed then the reader gateway will receive those commands and translate it into an rm command which is internally sending to the reader manager the rm server and similarly if there are uh reader or radio operation control commands like the changing your operating mode or starting the tag reads or stopping the tag reach those commands are translated and it's sent over to the radio uh control so the really gateway acts sort of like a switch and a translation logic uh but it also provides other functionalities that we talked about like the uh the tag retention and the batching and so on reader control is one that's responsible for interfacing with the radio this is the one that is reading the tags performing the filters determining what tag metadata to send based on the user configuration the operating modes that have been configured are also directly sent to the radio control so radio control is basically the one that is replacing llrp if you will uh in the conventional sense and that's the reason when uh iot connector is running uh llrp would be stopped internally by the reader so lr functionalities would not be available because radio control then uh takes over the final responsibilities or functionalities of nrp and then there is the uh reader manager this is the rm server that you might already be familiar with which provides the capabilities that are exposed by the reader today uh then there is the reader web server this is nothing but an http server and apache server that is hosted inside the reader this comes into picture when you are using the local rest what local rest really does is it provides an ability or exposes certain rest endpoints to directly interact with the id connector by talking or connecting directly to the reader so you can send http requests to the reader directly and perform or exercise all the iot connector functionalities directly from the reader if you don't want to go where an mqtt broker or any other cloud provider um so connect to deployment modes um so i earlier mentioned about the different interfaces we have there is a management interface there is a control interface there is a data interface and the monitoring interface now depending on how you configure it there are a bunch of deployment modes that are possible so in this case this is a pure cloud deployment you can see that the black arrow indicates management control and the red arrow indicates data uh in this case what would happen is that the reader uh would connect its management and control interfaces as well as data interfaces to a cloud service provider we provide support out of the box the gcp iot core aws id core the azure iot hub and the ibm watson idea so any customer application that wants to manage the reader in this scenario would lead to interface with the cloud service provider so if you if you are using say for example the gcp id core uh the reader is actually connecting to the encryption broker hosted by the ecb iot core and if the customer application wants to then uh manage that reader then it needs to interface with the gcpid core and send the data using the pops up uh service of the gcprd core and then or rather send commands using the gcp id code and then receive the responses back and any data that is sent by the reader is also sent directly to the iot core which the application can receive using the pop-up service so that's the pure cloud deployment and obviously this would require that the reader has the connection to the internet uh the second work scenario based on where the reader is deployed is called local deployment so in cases where the readers do not have internet access which i presume is in most cases when it comes to corporate uh deployments um it is possible to have the management and control interfaces locally controlled directly by the application using the local rest that i just mentioned and also the data can be consumed locally as well um so the customer application in this case would issue rest api request directly to the reader uh since http elastic is always a request response kind of a scenario that takes care of the management control and as far as data and management events are concerned uh those can be configured to be sent over to a another server either way that can be either be an http post server or just an http server and use the post method to send the data over or it can be an mqtt broker in which case the customer application would need to subscribe to the appropriate topics from the mbtt broker as well to receive the data from the reader we do support in when there is a local deployment there's a third data endpoint also possible which is called the keyboard emulation so in this case what would happen is the reader uh instead of sending the data over to um an mqtt broker or an http server using tcp if you connect the reader to a usb uh host of say a laptop or any other uh computer then the reader will act like a keyboard and it will start sending keys data as keystrokes into the uh the connected host so if there is a spreadsheet or a notepad that's open then you can start seeing all the tag data coming over into those uh applications that are running in the foreground so that's the keyboard emulation mode uh one quick thing i want to mention here is that yeah if you were familiar with fx connect as uh an application or a feature on the reader uh that sort of supported all these different modes the http mode the um http reports the keyboard emulation and and sort of thing so iot connector is now bringing into its umbrella all the functionalities that fx connect supported so fxconnect i think is going out of life which will be replaced by the rt connector um and that's what that's where the keyboard emulation has coming to the ito connector as well from the fx connect world all right uh and then there's the hybrid deployment um so if this is the case where [Music] if there are say for example if on-prem is too complicated uh if you don't want to have uh maybe if there are multiple sites for example and if there are readers deployed in each of the site you don't want to have a separate on-prem server managing the readers on that particular premises but you want to have a centralized management server but then you want something to uh consume the data locally so that sort of a scenario is possible in the hybrid deployment mode where the management and control are connected to a cloud provider be it a gcp aws or ibm or azure and the customer application can control these readers from the cloud providers but then data can be consumed directly or sent over to any other local interface that can be http post or keyword emulation or mqtt uh broker uh so in this case if the additional network bandwidth uh utilization that we that would have been um that would have occurred if had if the reader was sending the data to uh the cloud is all avoided because the data is now going local um and also in terms of the mqtt uh broker right so if there is a on on-prem mqtt broker to which the reader is configured to send data there are two different protocols through which we can send data over to mqtt uh there is a tcp protocol which uh regular on which is their regular entity protocol and there's the amputated your web sockets um so that's also supported in the uh id connector uh mktt or websockets is usually preferred by uh some corporate deployment corporati departments because that provides the ability to send data over ports 80 and if it is mqtt over secure web sockets you can send data over port 443 which is the standard https port um and that is usually favored by uh most writing departments because then they don't have to open up additional ports like in case of mqtt tcp they will need to open up the default port 5kgtcp which is port 1883 and even if it is uh we also support a secure mqtt over tcp which is mqtt tls uh but that again would requires that the port triple h3 is open because that is the default port for the liquidity or tls but if you are using a websocket or a secure web socket empty your websocket or secure web socket then you can work with ports 80 or 443 which is um which doesn't require which in most cases are open and more more corporate networks all right so that's the uh hybrid deployment uh the other deployment scenario that is also possible is the dual data path uh deployment uh where like i said we support two simultaneous data endpoints um the reader can now be managed and control from the cloud but then you could have the data going out to the cloud as well as to a local uh server the local server can again be http or keyboard and in case of by default if you had two different end points uh with all the data that is sent over to one end point is also centered to another endpoint so in the absence of a da application or the data analytics application uh if you can configure two separate data endpoints both of them are going to be receiving the same data they'll see the same data basically and that's what is illustrated here as well okay um moving on um so yeah i have mentioned about the different interfaces there is a management monitoring and control uh the data interface will be just a pure um data going over the data interface but in terms of management monitoring and control what are the different things that are supported by id connector um so if you look at the management functionalities um you can perform operations like getting the information about the reader that includes the status of the reader the network configuration and the region configuration of the reader and when i talk about status you can get all sorts of information like the uh you can get the cpu utilization memory utilization um disk utilization you can get the ntp status um you can get the antenna connection disconnection status um so all that is uh possible to be retrieved by the status api uh network will obviously tell you whether you're in a static ip configuration or enterprise or a dynamic ip configuration and it will also give you the current typegraders of the reader the region will tell what region the reader is configured to and so on and then the configure uh you yeah in addition to getting the information you can configure the reader as well uh in in terms of what can be configured on the reader you can send a xml profile to the reader this is similar to what you get in the profiles page of the dataweb console today so the entire reader configuration can be exported as a single xml file and then can be imported back the same xml content you can copy and paste into the set config command which provides the the ability to configure the reader using that xml uh you can also configure the end points from the iot connector itself so once i mean for the first time you want to connect the iot connector to some endpoint you would need to enter that from the web console but once it is connected if you want to change the endpoint configuration if you if you say for example don't want the data to be sent to the endpoint that it is currently sending to if you want to change that uh that you can change from the id connector as well uh you can change the way the events are uh configured uh basically events are centered by default these are and when i say events i'm talking about the management events here uh these events are sent out once every 60 seconds by default uh but if you don't want that behavior if you want to change the interval at which the uh management events are sent out you could reduce the interval uh but also you can customize what events you're interested in um so if you're say for example you don't want you don't really don't care about cpu or memory utilization then you can tell the reader don't tell me that information uh just send me the antenna connection disconnection status that that it will send so that sort of a customization can be done for uh how you want to receive the management image uh using the iot connector interfaces you can also configure the gpio led module so this is with the introduction of iot connector what is now also possible is that you can iot connector provides a rules-based management or control of gpos and leds so you can set up uh events or rather you can configure what events you are interested in and how depending on what events are triggered uh how you want to control the gpo and leds and reader will autonomously once it receives that configuration it will autonomously control the gpus and leds according to your configuration without having for you to actively control that from the application i'll also demo this in a little bit um and then yeah using the uh management functionalities you can manage the user app so you should you should be able to install uh user app start and stop the user apps uh and install them um that sort of thing and you will be able to update the firmware as well uh using the management functionality uh what is available in so in terms of control uh you can set the operating mode of the reader that is how the reader will read tags and how it will report tags and how often it will report tags you can also start and stop the diagrams on the reader so if you don't want to listen from this reader anymore you can simply stop ask the reader to stop and stop reporting tags um and also you can yeah that's on the control interface on the monitoring interface uh and like i said this is uh an asynchronous interface uh and these two are synchronous interfaces where you need to send a command to get back a response so on the asynchronous monitoring interface what you would get from the reader are the following events so there is a heartbeat event like i said it sent out once every 60 seconds um and you can change that behavior if you want but that that's that's the default there are gpi events uh so if there is gpi transition that has occurred on any of the uh gps of the reader and that will be sent out as an event on the manager interface as well so that uh if there is an application that is interested in uh knowing uh this event that it can process that message on the mqtt or http post server and uh yeah there are errors and warning events as well so so these these typically what would happen is in case of uh say for example when you want to install a certain user on the reader uh the mechanism that is supported for today for user app uh installation is that the user app when you send a command to the reader to install a user app the user app is itself must be hosted on a https server and the reader will pull the user app and then install it but in case there is something going wrong with the user app installation for example uh you would get back an error event on the management channel indicating that this this failed or something went wrong here uh similarly warning events are also available and uh firmware update progress uh events are also uh centered on the monitoring interface so once you're once you issue a formula updates uh start command uh the reader will send back a success response saying that it is initiating a firmware uh update but then the progress itself is reported back using the firmware update and if it fails in between uh the error or warning events are sent on the monitoring interface so this is uh what is supported in id connected today in terms of the management control and monitoring all right now in terms of the operating modes or reader configuration this id connector simplifies how the uh or how you configure the reader operating modes uh quite a bit so i think one thing that we're introducing the latest release is also the ability to configure the deployment environment so typically when there are okay there are three different uh environments possible there is a low interference environment this is a high interference environment and there's a very high interference market uh typically low interference environment is when you know for sure that there is not going to be much of any difference let's say for example you have only one um rfid reader and there's no other readers nearby and there are no other sources of rf interference near the reader in that case you can configure the environment variable to the low interference and you would the loop will automatically configure itself based on the region it will configure the um the link profiles and everything uh automatically and you wouldn't have to configure that explicitly for yourself there is a high interference environment where if and this would be the typical uh case where if you if the reader is going to operate along with other readers which is most likely the case uh then it is recommended that you configure this to a high interference environment this is also the default on the reader so if you don't configure anything it'll automatically default to the high interference environment and then there's a very high interference environment the very high interference environment is it's supposed to be picked when the readers are very densely packed together um so if there are more readers than there are available channels uh then typically you should select a very high interference environment uh the reading rates obviously would uh depend on what environment you're configuring so a low interference would probably give you a higher rate rate compared to a high interference or a very high interference and so on um and then yeah so this environment configuration is also possible uh to be made by the operating mode uh so that you can configure the environment configuration as part of the operating mode configuration and like i said the operating mode itself there are uh four default out-of-the-box operating modes supported and there is one custom mode which is uh an expert level or advanced level operating mode uh the four operating modes supported are simple inventory conveyor and portal these uh are targeted towards various different kinds of use cases in case of a simple mode it will just uh it will start reading tags and it will read forever and it will report only unique tags um that's the simple mode you can there are some customizations or norms available for you to change the behavior say for example you don't want all unique tags you want to filter out some things uh you can do that um and if you you can select the antennas on which you want to uh perform operations if you have more antennas and you don't want to read on all antennas uh you can select the antennas on which you want to read tags that's all possible using the simple mode same thing with the inventory mode in case of inventory mode in addition to the reader reading tags it will report back uh it will do a periodic interval a periodic uh inventory if you will so basically it will cycle through all the antennas and whenever it reads tags all the unique tags are reported and then you can configure the reporting interval and even though the unit tag was reported back then it will report it again after that interval has expired so that's what inventory mode does and in addition to just the reporting uh parameter you could also select uh filtering uh antenna powers or which antenna you want to perform the inventory on all that configuration is possible using the inventory mode as well and you get additional tag metadata in case of inventory mode as well so say for example you read tags and you get you the if the reader reports one unit tag and if the reporting interval is set to say one second the same unit tag is reported back after one second but then it also reports uh the number of reads or the number of times that tag was read between the last report and the next successive report so that sort of functionality available on the inventory then there's a conveyor mode conveyor mode uh reads tags on all antennas by default and it reports stacks per antenna basis uh and they can you can apply the any sort of filtering that you want you can select the antennas on which you want to perform inventory uh the same configurations that are available in the simple end inventory is also available in the conveyor mode in the portal mode you could basically trigger tag reads based on a gpi trigger so you can pick the gpi that you want to trigger tag reads on and whenever the gpi goes to the configured state the reader will start reading tags and start reporting unique tags and by default um it will report it will keep reading tags until no more unique tags are read and if after that it will wait for three seconds and stop uh reading tags but if you don't want that if you want to change the behavior if you stop if you want to stop maybe 500 milliseconds after uh or rather one second after the gpi is triggered you can configure that and gpa the reader will basically read for one second after the gpi is triggered and report all you need text that's the portal mode and custom is basically it'll throw open all the doors for you you can uh pick and choose any configuration twist all the knobs that's uh completely up to the user that's an expert level configuration um i will also show in terms of documentation what are the different knobs available uh i'll do that later once we run through this presentation um so yeah once you have once you know what operating mode to pick uh you can configure that operating mode uh in one of two different ways one is you can do that from your reader web console itself there is a new web page that is added to configure the operating mode on the reader or you could do that from the hierarchy connector interface um and that is basically if the iot connector control interface is set to mqtt for example you can change the operating mode using the mqtt interface itself um now so like i said uh the iot connector provides a rules-based mechanism through which you can control your gps gpus and leds um in order to do that and again i'll run through this in the video in some time or rather the demo in some time uh but what this allows the user to do is the user can select the default state of all the gpus and leds whether they want all the gps to be low or high or if the led want should be off or it should be glowing a particular color you can select all those defaults and once you've selected the defaults uh you need to basically pick the event of interest and there are few predefined events uh that are available so you can pick the event of interest and whenever that event is triggered um there are conditions that you can specify uh and if those conditions are met the actions that you configure will be performed by the reader automatically um see for example and what what one of the scenarios could be that whenever the gpi 1 is triggered and if the radio is reading tags if i want to turn the led red or green or if i want to turn the gpo to some state i could do that uh automatically so once that configuration is set and uh saved on the reader it will keep monitoring for those events and keep performing those actions as long as id connector is running so yeah this i'll show you how to configure that from the um page as well okay so that brings us to the uh user application the da app framework um so basically the idea here is that the da apps are going to be user written uh applications uh that can be written either in python or node.js and can be deployed on the readers once the applications are deployed on the readers you can see that this introduces the new uh component in the uh way the iot connector functions so instead of the reader gateway directly consuming data from the radio control and setting it out now the flow of the data will change the radio control will now start reading tags and send data to the analytics script first and let the analytics script do its uh whatever business logic is embedded in that script and then the analytics script can send the data over to the radio control or rather sorry the reader gateway and the reader gateway will send it out so the all the analytic script in this case need to worry about is how you want to handle your data if you want to reformat your data you can do that it doesn't need to worry about how the how to send the data out to any other endpoint it doesn't need to worry about rather it doesn't need to worry about things like what happens if the data data connection is lost because the reader gateway is then going to batch and retain that data and send it over when the connection is restored so all the goodies of the reader gateway that are otherwise available are still available when the analytics script is running and the analytics script is solely responsible for processing the data that the way you want to uh that's one uh thing that analytics script can do now the other application or other use case that the analytics script can satisfy is also real-time control so in in many cases what would happen is um you would want to typically control the gpos on the reader uh based on certain uh events so say for example if you read a particular tag that you were interested in and if if the idea is to control the gpu whenever that tag is read all that business logic can be embedded within the analytic script and because of the the nature of the analysis script and because it is running on the reader itself it can directly uh control the gpu in real time and uh the gpu event now doesn't i mean the gpu doesn't have to be controlled by an external application it can be done on the reader itself um and also i think like i mentioned in previous uh slides as well you the analytics script can be used to send uh different data to different endpoints so we support two different endpoints uh data endpoints on the reader uh if the idea is to send all data to one end point or unknown under only some filter data to a second endpoint the analytics script will come into the picture here and it can filter out all the data that it doesn't want to send to the second endpoint and send only that data that it's interested in to the second endpoint so that is uh possible as well and also it can reformat the data so the data that is coming out of the radio control is coming in say the json format uh say you do you're not interested in receiving json format data uh say you want key value pairs instead of json format and that probably comes into uh comes in handy when you're doing say keyboard emulation for example if there's a spreadsheet and you want to directly insert uh data such that it is um uh listed in in different columns automatically so you you can format your data accordingly and send it out read the gateway will promptly send it out to the keyword emulation endpoint or any other endpoint that you might have configured so all that can be done by the analytic script um so the analytics script in itself uh first of all needs to be makes use of what is called the da library um again we there are two different uh language bindings that are provided there's a python module in the node.js model that is onboard on the reader itself which the analytics clip needs to import and once it has imported those modules it can there are apis available uh for both python and node.js using which the analytics script can do its thing it can receive the data from the radio controller it can send the data to the reader gateway it can control the gpu uh everything that's possible from the analytic script can be done using the library um yeah what the library also does is it completely abstracts the underlying connection so that analytics script doesn't need to worry about how do how do i connect to radio controller how do i connect to real gateway like in the previous slide we were talking about it places itself between these two the script itself doesn't need to worry about it once it is included the da library uh once the python or the javascript is it has imported the appropriate module the module internally is taking care of establishing those connections to the appropriate components and the uh the user script as such doesn't need to worry about it um so once the nda library is written either in python or node.js and it has imported the da library and it knows how to transform the data all the logic is written the analytics script must be packaged and installed on the reader uh this will be very similar to how the other embedded user apps will be installed uh so it it needs to be packaged in the debian format uh similar to other embedded applications and you can install the application itself uh again using two different methods one is from the reader web console there's the applications page you can go and browse for that application and install it or you can install the application itself from the uh the iit connector management interface so you can host the application somewhere and send a command to the reader to download and install it and the reader will do that okay so this is the library interface um there are a few different uh i mean i just want to illustrate illustrate here what is minimal minimally required by the user applications to do uh the first thing that will be required is to create the citc object so and this is i'm taking a python as an example here so what would be required is once the script is has imported the crtc module uh it needs to instantiate or initialize the library using the grtc call and then once it has done that it needs to register itself to get a message call back or get a call back whenever a data event from the radio control is received and the library will take care of calling the uh callback um whenever the it gets a message from the radio control then there is the run forever so the python module makes use of what we call the async i o module um or rather what is uh popularly known as async io module and this is an asynchronous loop so the entire application is run as a asynchro event loop so you need to start the loop uh once you've registered your callbacks um the user application is then supposed to start the loop by calling the run forever this will uh cause the callbacks to be called by the da library whenever there's a message from the data control and what and there's the other uh function called the send next message what this will do is basically uh in your callback whenever the python or rather whenever the message callback is called by the da library uh the library itself can then process the data as it needs and then once it is ready to send the data out it can simply call send next message uh and specify the type of message it is sending and what is the message that needs to be sent out once that is done uh the reader gateway will take care of sending the message appropriately so these are at a very minimal these are the four api calls that need to be made uh in order to write a da application uh uh i'll i'll just walk through a helpful blown script and show you how simple it is now the other thing i talked about when we said we want to control the gpi or gpus as well there are that can be done by using the send next message uh there are you see here there is one argument called message type there are three different message types uh that are available one is called a data message then the other one is called a control message and then the third one is called a gpu message now data message is simple uh whenever the da application gets a callback from the radio control or gets a data event from the radio control your processes are processed the data appropriately and then when you want to send it back you just send it back and set the type to data and the reader gateway will then know that this is a data uh message from the da app and it needs to be sent to the data endpoint uh and the data gateway will take care of it the second type of message that is possible is the control message now this message uh okay so in in some cases it might be useful for the da app itself to send out say uh heartbeats for example if you want to monitor the state of the app and the application on the other side be it in the cloud or on-prem can monitor the state of the va app and take actions appropriately not just the monitoring but also if there are any additional management events that need to be sent out to the external application that can be also sent out using the control type message when the data gateway sees that the control my type message has been sent from the da app it will send it to the management interface instead of sending it to the data interface so the the application that is managing the reader will actually get that uh message from the the app and then the third type is the gpo event message type when you send the gpo message type it will uh directly uh result in a gpo being controlled or the reader itself this this type of message is not sent out from the reader so those are the three message types um so this is a very simple small uh sample da app uh written in python as you can see there are uh basically at a high level what this application is doing is it is filtering the tags based on a prefix so if the tags start with the numbers four and three then it will send it out if it doesn't then it will just drop the tag uh that's what this application is doing uh so you in order to do that there is a new message callback uh which is uh implemented by the user application uh here i think it what it is simply doing is checking the type of message that it has received in this case it is the tag info and it's in the json format that's the enumeration for the message type that is being sent to this module or the callback and then it is parsing that information and it is checking the getting the id tag id of that in that event and checking if it starts with this prefix if it is starting with that prefix then it will send the message out to the reader gateway uh by setting the message type to data if it is not starting with the prefix it will simply drop it that's what this is doing uh now you might have noticed there's a second callback here this is called the pass-through callback um now what this will do is you can they it may be required for the da apps uh or in some cases it may be the use case may be that the client or the some management application which is managing the readers they want to send a command to the da app instead of sending it to the reader itself now if you want to send that command to the da app then there is a separate iot connected command called the set data to user app that can be used by the applications and when once the da app registers a pass-through callback that command when received by the reader gateway we'll send it directly to the app and this callback will be invoked so in this case what this password callback is doing is it is simply changing the prefix so in this case it's a dynamic filter so if by default this application will keep filtering on prefix 43 but at some point you decide you want to change the filtering prefix you don't want to filter by 43 but maybe by 86 you could simply change the prefix without having to rewrite the application by sending a command using the password callback uh some and the command actually will be sent to the reader gateway the reader gateway will then send it to the app and when it sends to the app it will automatically issue a callback to this method where it is then changing the prefix that's what this is doing all right uh so that's all i had in terms of the slides that i wanted to run over i just want to quickly want to share the documentation and where you would or what we you have coming in terms of documentation and what i would do is instead of looking at the on current online documentation i want to run quickly over through what you can expect to see in the near future um so and this is the documentation format that we are looking at as well so this is uh the most of the sections that you see here is already there in the online form documentation but we are a communication with some uh additional sections as well so you can see that uh the regular sections about the system overview that's all there the deployment modes are explained here as well uh and then there's the operating modes that are explained here so you can see what each of these does uh in terms of uh tag reports and all that and so on and then there is the um how do you start tag reads basically so that's what this section is it's a quick start guide uh you can configure some end points and start tag reads and then there is the how to guides these guides specifically walk you through how to connect to the gts for example aws tcp ibm azure mp3 dhtp post if you want to do that there's also a section on how to change the gpi led configuration using the reader web page which i'll just walk through in a little bit and um yeah there's a separate section on the user apps um this is where you can expect to see the explanation of different user apps there's a python programmers guide which will walk you through how to write a simple user application um we are still working on the node.js but that will be there as well and there's a package in deployment uh which is which will tell you how to package the application how to deploy it on the readers and so on uh we've added some sections on certificate management and we are also coming up with some migration guides like i said the fx connect is going to be end of life and if there are customers out there who are using fx connect and they want to migrate over id connector uh this section would help uh figure out how to go about that migration in terms of what you had from fx connect and porting it over to id connector um and then these are all uh sections that you had before as well so yeah that's what i wanted to share in terms of the documentation um okay maybe i just want to quickly show the quick demo as well um so in the new reader interface going to go to the communication zebraity connector these are the four new pages that we've added there's a configuration page where you this will allow you to add different endpoints and configure different endpoints you can map the endpoints to the different interfaces as well uh if you go to the add endpoint these are the currently supported endpoint types on the reader you can see if i go back to configuration page i've already added two endpoints here there's one mqtt and the http post server i can uh go ahead and if i want to add aws or http ibm or any other endpoint i can do that as well so for just for them i'll just quickly walk through how to configure an mqtt uh endpoint so in this case what would happen is you need to specify a mqtt broker server address uh the name is mandatory so you can split and then followed by you can specify the server ip or in this case if you want to use the publicly available emqx broker you can do that by typing empty stop broker io the port number that you want to select uh this is 1883 by default for the emqxi uh public broker and like i was mentioning earlier we there are these different protocols that are supported with the mqtt broad broker as well so there is a tcp protocol which is over uh uh portal 1883 without any security there's an infiniti over tls uh in which case this would be port triple h3 this would enable uh encrypted um and authenticated message communication between the competitive broker as well as the reader then in addition to that there's a web socket and we have a secure web socket that you can configure on the reader so i'll maybe select tcp here 1883 so once you've added that you need to specify a client id typically the client credit can be anything you want uh but in most cases i just provide test um and then once the connection parameters are configured you can go and configure the topics that need to be uh that the reader should subscribe to and that you can expect the reader to send data to in like i said in earlier there are two asynchronous interfaces and two synchronous interfaces the asynchronous are management and tag data data events and there is only one topic and the reader sends data to this topic so in this case the reader will be the publisher and any application that is interested in those events will be subscriber but in case of synchronous interfaces like the management and control interface there's a command topic and a response topic you need to configure the command topic and the reader will subscribe to this topic and the application that is interested in managing the readers will need to publish to that topic and there's a response topic to which the reader will send back the response so reader will be the publisher in this case and the application that is managing the readers would lead to the subscriber and the same is true for the control uh interface as well so if you want to uh quickly do like um uh you can configure topics maybe i'll skip this portion the interest of time i'll go over to this i'll show you what i have added here in my input that i've already added um this is the connection that i have set up and these are the topics that i've configured there's a monitoring data command and response uh for management and then there's a command and response for the control and once i've added this topic um it'll show up in the table over here i can similarly add a http post topic uh by going over to this select http post set the url and i can there are a bunch of different authentication modes that are supported by the reader you can select any one of those in my case it's none uh and if you are using https you can select certificates as well so that's already added on my reader so i'll just walk through that quickly and what i've done here is if you like you can see there's a management interface which i have configured to the emqx broker there's a control interface which i have again configured to the emqx broker and there's a tag data interface uh there are two tag data interfaces one and two and you can configure them to either the inks or http post now you can see that the http post is only visible in the asynchronous interfaces because that's not supported for synchronous interfaces um so that you can select one is actually http post which is a web hook and the other one to an mqtt so the reader will send data to both of these endpoints simultaneously and the management events are sent over to the emg explorer in addition to that i have also checked here the local rest management and control what this will provide me is an ability to control the reader locally but also control the reader using the mktt broker so i can send using postman an http request to the reader to get say the version for a copyrighter for example and i can also do it from the integrated broker so there is dual management interface if you will and dual control interface uh and also dual data interface so once all of that is done you click on it and update and come to the connection page and click on connect it will connect all these enter points in my case the reader is already connected to all of these i can quickly demonstrate how to use a local rest for management control in order to do that the first the reader the user must log into the reader the user reader will return a java web token so if i send for example it i get a get back a new token which i need to use in the further request as a beta token authentication method but in order to get this token itself i need to use a basic authentication and use the um the admin and username and password that is used for my reader web console the same username and password must be specified so once i get back at token so if i want to do a get a status for example because of the set of the environment variables such that it automatically substitutes the token over here and the authentication is better token i send a request i get back the data status so you can see the internet status the fly cpu flash uh all different sort of statuses here um i can also start and stop tag reads so say for example if i want to start tag range now the reader is reading tags i can i have subscribed this mqtt client to the entity broker that i've configured um i also want to show here the web hook site so you can see that now the dealer is sending data to two different points one is coming over to the web book site as http post the other one coming over to the mqtt broker um where the mqx amputated broker um then i can issue a stop and it will stop reading tags that's basically how you can start and stop tag reads on the reader and see that tag data then i think the couple of other pages that i want to quickly walk through was the operating mode this is the one where you can change the operating mode of the reader you can select the one of the four different modes that we talked about the simple conveyor inventory importer in case of the inventory and portal there are additional options available which are called more specific settings uh you could select in case of portal what gpi uh trigger you want and what state it should be in uh for it to start reading tags and uh the stop interval how long you want the reader to read tax after the interval or the after the trigger has occurred the same thing with inventory you can configure the duration at which the inventory will keep reporting tags back to the data endpoint uh custom mode you can configure in this case this is since all the knobs are available this is completely a text box that you can simply type a json object into and click save and it will save that on the reader and i in terms of environment configuration i talked about the three and different environment configurations they're very low low interference high interference and very high influence these are available here from the operating web page uh we can specify the tag id filter you can if you are looking for tags with a certain pattern you can select that there is a prefix suffix and a rejects kind of a match available so if it's a prefix it will report all those tags that start with the prefix if it's a suffix it'll report only those tags that end with that suffix and if it's a regex it will do attack pattern matching and report only those tags and you you also can select whether you want to report those tags or drop those tags here include and exclude operation will either report those tags or drop the tags there's a report filter duration as well that you can select which tells how frequently the unit tags are reported back to the data endpoint and you can change the select the antennas that you want to perform the operation on and then transfer power so that's the operating mode uh quickly i want to cover the gpio configuration here the gpio led configuration uh this is the page where you can select or or how configure how you want the repair ready model to work um in this case what once you select the defaults of each of these uh individual gpus and leds what would uh the user need to do is select the event of interest so by default these are the events that are supported so if you're interested in a gpi event for example you can select gpi i'm interested in gpi2 and i want to know whenever the gpi2 is for low and if the gpi2 is pulled low and if you want to take some action but base it on a condition say for example you want to do it only when the cloud is connected so if the gpi 2 is low and if the cloud is connected uh and if you want to control the gpi gpu one too high for example you can do that so once you click on add it will show up here in the below table which will indicate what the added configuration was so you can add multiple such configurations and keep adding and then click on save which will eventually be saved on the reader uh in terms of the conditions you can select more than one condition so if i go back to uh this one here so we set gpi1 and we want to take action only when cloud is connected but i also want to check radio ongoing status um and i can say select an operator either i want both these conditions to be met or one of them to be met uh you can select the operand here and then select the operation that you want to perform whether it's a gpu control or an led control um and the reader will autonomously take actions based on what is configured over here um all right so i think that sort of uh completes what i wanted to cover on the iit connector not sure if we have sufficient time to take questions but james back to you thank you very much harsha that was a great presentation very thorough much appreciated um so as you'll probably realize we have overrun the session slightly so we're just eating into the break but we have plenty of questions so we're going to spend uh six or seven minutes or so getting through as many of these questions as we can and then as we've done for previous sessions we'll put together a q a document afterwards uh with any questions that we didn't get to so let's get started and get through as many as we can so first question harsha i have used iot connector but i didn't find a way to activate antenna one and two with gpio one and antenna three and four with gpio2 is it possible uh well unfortunately that's not supported yet um so i think you basically if i'm not wrong you're asking about an operating mode configuration something like a portal where you want to have multiple gpi triggers and based on that gpi trigger you want to select which antennas the operation will be tags will be read on and if that is correct uh if my understanding of your question is correct unfortunately that's not supported the portal mode only takes one gpi for now uh there has been an ask in terms of how we can support multiple gps as triggers but that's probably sometime in the future okay thank you um is there compatibility with microsoft azure iot platform i.e are we planning to be microsoft's or iot certified uh yeah i think in the configuration page if you go to the add endpoint that's one of the uh endpoints that we support as well the azure iot hub so we you can specify the hub name the port that you want to use i think i azure only always supports triple h3 which is the secure mqtts protocol uh the device name and debug and then you can also also configure the topic so that's already supported on the id connector excellent thank you um how many reads can iot connect the buffer is that 60 000 uh well it's slightly higher on the iot connector but in term when it comes to iit connector the reads are different when compared to llrp like i said earlier in case of lnrp each time read is buffered or every tag report is buffered or ported back but in case when we when we talk about iot connector uh what we really talk about is not a tag read or but rather a data event and a data event itself can be generated after multiple tag rates right so for example when i say um if i'm operating in simple mode for example simple mode will continue to run forever and it will keep only reporting unit tags and since that's the only data event that goes out uh no matter how many times that is read how many times that unique tag is read not every single repo tag read is reported back it only reports the united once now because that is the only data event that's going out and because the id connector can buffer up to 150 000 uh events the number of uh tag reads or which that gets the buffering gets applied is really huge and it all depends on how your operating mode is configured and what mode you are on what reporting interval you have set for example set for example um so depending on that yeah the default uh maybe i can quickly show that here as well there's a batch and retention uh tab here where you can select how many tags you want to retain uh as opposed to lnrp so 150 000 is the max allowed but you can configure it um also you can configure the throttle uh the rate at which you want these tag retain tax to be sent back once the connection is restored uh but like i again said the the tag the data events in case of iot connector do not exactly correlate one to one with how llrp buffers the tag there there it's much more okay great thank you uh so uh is there a specific firmware version required to use iot connector on the fx 75 and 9600 uh yeah that is correct so i think um we the iot connector was already available in the former version 3 30 that we have on the support side um although not all of these pages were there that was more a better level functionality rather than a full uh official launch the upcoming release that we are making uh hopefully in a few days a week uh would be a more official launch of the id connector and the the firmware version that you can expect to see is 32120 perfect thank you um the current 3.10.30 firmware has keyboard provisioning oh it doesn't uh that is something that we've added uh in the that's going to be out in their next community okay perfect um what forms of authentication does iot connect the support for the readers both over http as well as mqtt specifically is oauth supported uh well oauth is not supported in but for http we support other different forms of authentication uh the basic authentication is supported only digest ntlm and nsa are supported uh there is a tns we what we call the tls authentication type basically this is a certificate based authentication uh where you could configure the reader to connect to the other endpoint only if there is a uh mutual authentication enabled that is if the reader knows about the ca certificates of the endpoint that it is talking to uh so yeah these are the ones that are supported what is not supported yet okay great thank you uh given that google cloud iot core is being retired in august 2023 has zebra started testing with clear blade which is pointed to as the replacement for google cloud iot core um so yeah i think we have been discussing internally on how to handle the uh the retirement of the gccbi decor the talk is i think they would want to move towards a grpc based protocol i think that's what we are looking to uh support as well but there's no clear timelines as to when we would be ready to support that officially on the id connector understood okay probably got room for two or three more questions so uh does the iot connector have to be used uh will it be the only way to connect to readers in future can developers use the same methods they currently use to connect to readers so i i think as far as the uh the fx and atr readers are concerned uh iot connector will be yet another interface on top of the currently available interfaces so you would be able to use whatever mechanism that you are currently using to talk to the reader and continue to use it but i think in the future readers we might the the direction that we are heading towards is to standardize iit connector and um move to an id connector only interface by default okay so with the fx 9600 using iot connector how do you configure the time in which the reader can react to internet disconnection at this period of reaction usually 30 seconds the readings are lost does that make sense um okay so i think uh it's the question probably uh means how do we know when to uh when the data endpoint has disconnected and start uh the retention on the reader itself um so i think by default the uh http post endpoint i forget the timeout on it uh but 30 seconds is close to what we have um maybe i can get back to the exact timeout but yeah what would happen in the in case of readers is if there is a data an event that needs to be sent out the reader would try to send it to that data endpoint whether it's enquiry or http post and if that attempt is unsuccessful if it fails for some reason then the reader will save it and the reader also preserves the order so the next time someone gets back there is a data event received from the radio control we would attempt to send the previous event first and then send the next event and if they are successful then retention doesn't come into picture but if that fails then we'll preserve the successive data events as well so the exact timeout timeout is around 30 seconds but that's how we decide when to retain it and why not okay perfect so with that i think we're gonna have to leave it there we're gonna have to move on to the next session so apologies if we haven't got around to your question as i've mentioned we'll put together a document and send a link as so just like to finish up by saying thank you very much archer for your second session another great presentation hopefully everyone enjoyed it and learned a little bit more about iot connector i'm going to end this session now and move over to our final session of the day which is around 123 rfid desktop so we'll see you there in just a moment thanks again harsha thank you
