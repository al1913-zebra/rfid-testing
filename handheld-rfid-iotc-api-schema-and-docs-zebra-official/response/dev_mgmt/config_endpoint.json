{
  "title": "config_endpoint_response",
  "x-stoplight": {
    "id": "kw9xt7g73rjbc"
  },
  "type": "object",
  "examples": [
    {
      "command": "config_endpoint",
      "requestId": "abc123",
      "apiVersion": "V1.1",
      "response": {
        "code": 0,
        "description": "Success"
      }
    }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "config_endpoint",
      "description": "The command that was executed to configure an endpoint."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "The unique identifier of the original request."
    },
    "apiVersion": {
      "type": "string",
      "example": "V1.1",
      "enum": [
        "V1.0",
        "V1.1",
        "V1.21"
      ]
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml"
    }
  },
  "required": [
    "command",
    "requestId",
    "apiVersion",
    "response"
  ]
}