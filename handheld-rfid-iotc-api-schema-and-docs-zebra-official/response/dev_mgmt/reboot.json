{
  "title": "rebootResponse",
  "x-stoplight": {
    "id": "vdmaqk96eai0f"
  },
  "type": "object",
  "examples": [
     {
       "command": "reboot",
       "requestId": "123abcd",
       "apiVersion": "V1.1",
       "response": {
           "code": 1,
            "description": "Command payload is accepted"
        }
     }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "reboot",
      "description": "The command that was executed to reboot the device."
    },
    "requestId": {
      "type": "string",
      "example": "123abcd",
      "description": "The unique identifier of the original request."
    },
    "apiVersion": {
      "type": "string",
      "enum": [
        "V1.0",
        "V1.1"
      ],
      "example": "V1.1"
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml"
    }
  },
  "required": [
    "command",
    "requestId",
    "apiVersion",
    "response"
  ]
}