{
  "title": "control_cmds",
  "x-stoplight": {
    "id": "71accf05dd055"
  },
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "enum": [
        "control_operation",
        "set_operating_mode",
        "get_operating_mode",
        "set_post_filter"
      ],
      "description": "The control command to be executed on the device."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "The unique identifier of the original request."
    },
    "payload": {
      "oneOf": [
        {
          "$ref": "../refrence/payload/operatingModePayload.yaml",
          "description": "Represents the reader operating mode."
        },
        {
          "$ref": "../refrence/payload/ctrlOprPayload.yaml"
        },
        {
          "$ref": "../refrence/payload/postFilterPayload.yaml"
        }
      ]
    }
  }
}